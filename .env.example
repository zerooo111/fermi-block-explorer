# Fermi Block Explorer Environment Variables

# API Base URL
# For development with proxy (to avoid CORS): /api
# For production or local API: http://localhost:8080
VITE_API_BASE=/api

# WebSocket is not used - we use REST API polling instead
# VITE_WS_BASE=/ws
